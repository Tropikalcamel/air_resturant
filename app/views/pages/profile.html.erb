<!-- - create custom routes for profile
- create profile method in pages controller
- create view for profile.html.erb inside pages views folder
- current_user.bookings -->
<div class="container">
    <h1><%= @user.email %></h1>
  </div>
</div>

<div class="container">
  <div class="row mt-5">
    <% @user.bookings.each do |booking| %>
      <div class="col-4">
        <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('')">
          At:  <%= booking.restaurant.name %>
          Date: <%= booking.booking_date %>
          Time: <%= booking.booking_time %>
           <%= button_to "Delete", booking_path(booking), method: :delete, class:"btn btn-danger" %>
          <%= button_to "update", booking_path(booking) , class:"btn btn-primary" %>
          </div>
      </div>
    <% end %>
